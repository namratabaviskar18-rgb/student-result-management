# student-result-management
Student Result Management System — a command-line Python project that lets you add, search, and view student results. It takes marks for 5 subjects, automatically computes total marks, percentage, grade, and pass/fail status, and stores every record persistently in an Excel file (student_results.xlsx) using openpyxl.
